pyrogram==2.0.106
ipinfo==4.4.1
ipaddress==1.0.23
requests==2.31.0
pymongo==4.5.0
aiohttp==3.9.1

# Use pre-built wheels for these packages to avoid compilation
TgCrypto==1.2.5; platform_machine != 'aarch64' and platform_machine != 'arm64'
frozenlist==1.4.0; platform_machine != 'aarch64' and platform_machine != 'arm64'
multidict==6.0.4; platform_machine != 'aarch64' and platform_machine != 'arm64'
yarl==1.9.2; platform_machine != 'aarch64' and platform_machine != 'arm64'

# Alternative packages that don't require compilation
python-multipart==0.0.6
